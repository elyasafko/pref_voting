[metadata]
name = pref_voting
# ...any other metadata you already have...

[options]
# auto-discover packages
packages = find:

[options.packages.find]
# exclude the paper folder
exclude =
    paper
    paper_ignored
